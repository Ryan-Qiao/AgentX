## chunk 0

headingPath: [图解MySQL-小林coding-亮白版-v2.0.pdf]

sectionChunkIndex: 1

charRange: 0-1200

contentLength: 1200

```text
图解MySQL介绍

⼤家好，我是⼩林。

我是《图解MySQL》的作者，这是⼀份专注 MySQL 学习与⾯试的开源资料，内容都是整理于我的公众号⾥的图
解MySQL的⽂章。

简单介绍下《图解MySQL》，整个内容共有 20W 字 + 320 张图，每⼀篇都⾃⼰⼿绘了很多图，⽬的也很简单，想
通过「说⼈话+图解」的⽅式，让你轻松掌握 MySQL 的核⼼知识。

本《图解MySQL》PDF⽂档的内容，在我的图解⽹站也可以阅读

在线阅读地址是：https://www.xiaolincoding.com/mysql

适合什么群体？

《图解MySQL》写的 MySQL 知识主要是⾯向后端开发和 DBA的，因为⼩林本⾝也是个后端开发，所以涉及到的
知识主要是关于⽇常开发或者⾯试的 MySQL 知识。

⾮常适合有⼀点 MySQL 基础，但是又不怎么扎实，或者知识点串不起来的同学，说⽩这本《图解MySQL》就是
为了拯救半桶⽔的同学⽽出来的。

因为⼩林写的图解MySQL就四个字，通俗易懂！

相信你在看这本图解MySQL的时候，你⼼⾥的感受会是：

「卧槽，索引原理原来是这样的」

「卧槽，事务和锁的关系我终于搞懂了」

「卧槽，MySQL ⽇志我串起来了」

「卧槽，相见恨晚」

当然，也适合⾯试突击 MySQL 知识时拿来看。《图解MySQL》⾥的内容基本是⾯试常见的知识点，⽐如索引、事
务、锁、⽇志等等，也有很多⾯试常问的问题，⽐如：

为什么 MySQL 要⽤ B+ 树作为索引？

索引什么时候会失效？

事务的隔离级别是怎么实现的？

MySQL 可重复读隔离级别完全解决幻读了吗？

MySQL 有哪些锁？是怎么加锁的？

update 没加索引会锁全表吗？

redo log、undo log、binlog 有什么⽤？

….

不敢说 100 % 涵盖了⾯试的 MySQL 问题，但是⾄少 90% 是有的，⽽且内容的深度应对⼤⼚也是绰绰有余，有⾮常
多的读者跑来感激⼩林的图解MySQL，帮助他们拿到了国内很多⼀线⼤⼚的 offer。

要怎么阅读？

很诚
...
```

## chunk 1

headingPath: [图解MySQL-小林coding-亮白版-v2.0.pdf]

sectionChunkIndex: 2

charRange: 1050-2250

contentLength: 1199

```text
以随意阅读任何章节。

《图解MySQL》⽬录结构如下（别看篇章不多，每⼀章都是很长很长的⽂章哦  ）：

⼀、基础篇

执⾏⼀条 SQL 查询语句，期间发⽣了什么？

MySQL ⼀⾏记录是怎么存储的？

⼆、索引篇

索引常见⾯试题

从数据页的⾓度看 B+ 树

为什么 MySQL 采⽤ B+ 树作为索引？

MySQL 单表不要超过 2000W ⾏，靠谱吗？

索引失效有哪些？

count(*) 和 count(1) 有什么区别？哪个性能最好？

MySQL 分页有什么性能问题？怎么优化？

三、事务篇

事务隔离级别是怎么实现的？

MySQL 可重复读隔离级别，完全解决幻读了吗？

四、锁篇

MySQL 有哪些锁？

MySQL 是怎么加锁的？

update 没加索引会锁全表?

MySQL 记录锁+间隙锁可以防⽌删除操作⽽导致的幻读吗？

MySQL 死锁了，怎么办？

字节⾯试：加了什么锁，导致死锁的？

五、⽇志篇

undo log、redo log、binlog 有什么⽤？

六、内存篇

揭开 Buffer_Pool 的⾯纱

七、架构篇

MySQL的架构是怎样的？

有错误怎么办？

⼩林是个⼿残党，时常写出错别字。

如果你在学习的过程中，如果你发现有任何错误或者疑惑的地⽅，欢迎你通过以下⽅式给我反馈：

登记飞书表格：⼩林图解⽹站问题反馈登记（推荐）

⼩林抽时间会逐个修正，然后发布新版本的《图解MySQL》，⼀起迭代出更好的《图解MySQL》！

先在这⾥提前感谢⼤家了！

新的图解⽂章都在公众号⾸发，别忘记关注了哦！如果你想加⼊百⼈技术交流群，扫码下⽅⼆维码回复「加群」。

⼀、基础篇

1.1 执⾏⼀条 select 语句，期间发⽣了什么？

⼤家好，我是⼩林。

学习 SQL 的时候，⼤家肯定第⼀个先学到的就是 select 查询语句了，⽐如下⾯这句查询语句：

// 在 product 表中，查询 id = 1 的记录
select * from product where id = 1;

但是有没有想过，MySQL
...
```

## chunk 2

headingPath: [图解MySQL-小林coding-亮白版-v2.0.pdf]

sectionChunkIndex: 3

charRange: 2100-3300

contentLength: 1199

```text
MySQL 执⾏⼀条 SQL 查询语句的流程，也从图中可以看到 MySQL 内部架构⾥的
各个功能模块。

可以看到， MySQL 的架构共分为两层：Server 层和存储引擎层，

Server 层负责建⽴连接、分析和执⾏ SQL。MySQL ⼤多数的核⼼功能模块都在这实现，主要包括连接器，

查询缓存、解析器、预处理器、优化器、执⾏器等。另外，所有的内置函数（如⽇期、时间、数学和加密函
数等）和所有跨存储引擎的功能（如存储过程、触发器、视图等。）都在 Server 层实现。

存储引擎层负责数据的存储和提取。⽀持 InnoDB、MyISAM、Memory 等多个存储引擎，不同的存储引擎
共⽤⼀个 Server 层。现在最常⽤的存储引擎是 InnoDB，从 MySQL 5.5 版本开始， InnoDB 成为了 MySQL
的默认存储引擎。我们常说的索引数据结构，就是由存储引擎层实现的，不同的存储引擎⽀持的索引类型也
不相同，⽐如 InnoDB ⽀持索引类型是 B+树 ，且是默认使⽤，也就是说在数据表中创建的主键索引和⼆级
索引默认使⽤的是 B+ 树索引。

好了，现在我们对 Server 层和存储引擎层有了⼀个简单认识，接下来，就详细说⼀条 SQL 查询语句的执⾏流程，
依次看看每⼀个功能模块的作⽤。

第⼀步：连接器

如果你在 Linux 操作系统⾥要使⽤ MySQL，那你第⼀步肯定是要先连接 MySQL 服务，然后才能执⾏ SQL 语句，
普遍我们都是使⽤下⾯这条命令进⾏连接：

## -h 指定 MySQL 服务得 IP 地址，如果是连接本地的 MySQL服务，可以不⽤这个参数；
## -u 指定⽤户名，管理员⻆⾊名为 root；
## -p 指定密码，如果命令⾏中不填写密码（为了密码安全，建议不要在命令⾏写密码），就需要在交互对话⾥⾯输⼊
密码
mysql -h$ip -u$user -p

连接的过程需要先经过 TCP 三次握⼿，因为 MySQL 是基于 TCP 协议进⾏传输的，如果 MySQL 服务并没有启
动，则会收到如下的报错：

如果  MyS
...
```

## chunk 3

headingPath: [图解MySQL-小林coding-亮白版-v2.0.pdf]

sectionChunkIndex: 4

charRange: 3150-4350

contentLength: 1200

```text
，都
会基于连接开始时读到的权限进⾏权限逻辑的判断。

所以，如果⼀个⽤户已经建⽴了连接，即使管理员中途修改了该⽤户的权限，也不会影响已经存在连接的权限。修
改完成后，只有再新建的连接才会使⽤新的权限设置。

如何查看 MySQL 服务被多少个客户端连接了？

如果你想知道当前  MySQL 服务被多少个客户端连接了，你可以执⾏ show processlist 命令进⾏查看。

⽐如上图的显⽰结果，共有两个⽤户名为 root 的⽤户连接了 MySQL 服务，其中 id 为 6 的⽤户的 Command 列的
状态为 Sleep ，这意味着该⽤户连接完 MySQL 服务就没有再执⾏过任何命令，也就是说这是⼀个空闲的连接，并
且空闲的时长是 736 秒（ Time 列）。

空闲连接会⼀直占⽤着吗？

当然不是了，MySQL 定义了空闲连接的最⼤空闲时长，由 wait_timeout 参数控制的，默认值是 8 ⼩时（28880
秒），如果空闲连接超过了这个时间，连接器就会⾃动将它断开。

mysql> show variables like 'wait_timeout';
+---------------+-------+
| Variable_name | Value |
+---------------+-------+
| wait_timeout  | 28800 |
+---------------+-------+
1 row in set (0.00 sec)

当然，我们⾃⼰也可以⼿动断开空闲的连接，使⽤的是 kill connection + id 的命令。

mysql> kill connection +6;
Query OK, 0 rows affected (0.00 sec)

⼀个处于空闲状态的连接被服务端主动断开后，这个客户端并不会马上知道，等到客户端在发起下⼀个请求的时
候，才会收到这样的报错“ERROR 2013 (HY000): Lost connection to MySQL server during query”。

MySQ
...
```

## chunk 4

headingPath: [图解MySQL-小林coding-亮白版-v2.0.pdf]

sectionChunkIndex: 5

charRange: 4200-5400

contentLength: 1200

```text
ike 'max_connections';
+-----------------+-------+
| Variable_name   | Value |
+-----------------+-------+
| max_connections | 151   |
+-----------------+-------+
1 row in set (0.00 sec)

MySQL 的连接也跟 HTTP ⼀样，有短连接和长连接的概念，它们的区别如下：

// 短连接
连接 mysql 服务（TCP 三次握⼿）
执⾏sql
断开 mysql 服务（TCP 四次挥⼿）

// ⻓连接
连接 mysql 服务（TCP 三次握⼿）
执⾏sql
执⾏sql
执⾏sql
....
断开 mysql 服务（TCP 四次挥⼿）

可以看到，使⽤长连接的好处就是可以减少建⽴连接和断开连接的过程，所以⼀般是推荐使⽤长连接。

但是，使⽤长连接后可能会占⽤内存增多，因为 MySQL 在执⾏查询过程中临时使⽤内存管理连接对象，这些连接
对象资源只有在连接断开时才会释放。如果长连接累计很多，将导致 MySQL 服务占⽤内存太⼤，有可能会被系统
强制杀掉，这样会发⽣ MySQL 服务异常重启的现象。

怎么解决长连接占⽤内存的问题？

有两种解决⽅式。

第⼀种，定期断开长连接。既然断开连接后就会释放连接占⽤的内存资源，那么我们可以定期断开长连接。

第⼆种，客户端主动重置连接。MySQL 5.7 版本实现了 mysql_reset_connection() 函数的接⼜，注意这是接⼜
函数不是命令，那么当客户端执⾏了⼀个很⼤的操作后，在代码⾥调⽤ mysql_reset_connection 函数来重置连接，
达到释放内存的效果。这个过程不需要重连和重新做权限验证，但是会将连接恢复到刚刚创建完时的状态。

⾄此，连接器的⼯作做完了，简单总结⼀下：

与客户端进⾏ TCP 三次握⼿建⽴连接；

校验客户端的⽤户名和密码，如果⽤户名或密码不对，则会报错；

如果⽤户名和密码都对了，会读取该⽤户的权限
...
```

## chunk 5

headingPath: [图解MySQL-小林coding-亮白版-v2.0.pdf]

sectionChunkIndex: 6

charRange: 5250-6450

contentLength: 1200

```text
ySQL 就会先去查询缓存（ Query Cache ）⾥查找缓存数据，看看之前有
没有执⾏过这⼀条命令，这个查询缓存是以 key-value 形式保存在内存中的，key 为 SQL 查询语句，value 为 SQL
语句查询的结果。

如果查询的语句命中查询缓存，那么就会直接返回 value 给客户端。如果查询的语句没有命中查询缓存中，那么就
要往下继续执⾏，等执⾏完后，查询的结果就会被存⼊查询缓存中。

这么看，查询缓存还挺有⽤，但是其实查询缓存挺鸡肋的。

对于更新⽐较频繁的表，查询缓存的命中率很低的，因为只要⼀个表有更新操作，那么这个表的查询缓存就会被清
空。如果刚缓存了⼀个查询结果很⼤的数据，还没被使⽤的时候，刚好这个表有更新操作，查询缓冲就被清空了，
相当于缓存了个寂寞。

所以，MySQL 8.0 版本直接将查询缓存删掉了，也就是说 MySQL 8.0 开始，执⾏⼀条 SQL 查询语句，不会再⾛到
查询缓存这个阶段了。

对于 MySQL 8.0 之前的版本，如果想关闭查询缓存，我们可以通过将参数 query_cache_type 设置成 DEMAND。

::: tip

这⾥说的查询缓存是 server 层的，也就是 MySQL 8.0 版本移除的是 server 层的查询缓存，并不是 Innodb 存储引
擎中的 buffer pool。

::::

在正式执⾏ SQL 查询语句之前， MySQL 会先对 SQL 语句做解析，这个⼯作交由「解析器」来完成。

第三步：解析 SQL

解析器

解析器会做如下两件事情。

第⼀件事情，词法分析。MySQL 会根据你输⼊的字符串识别出关键字出来，例如，SQL语句 select username from
userinfo，在分析之后，会得到4个Token，其中有2个Keyword，分别为select和from：

关键字

select

⾮关键字

username

关键字

from

⾮关键字

userinfo

第⼆件事情，语法分析。根据词法分析的结果，语法解析器会根据语法规则，判断你输⼊的
...
```

## chunk 6

headingPath: [图解MySQL-小林coding-亮白版-v2.0.pdf]

sectionChunkIndex: 7

charRange: 6300-7500

contentLength: 1200

```text
QL 解析器就会给报错。

但是注意，表不存在或者字段不存在，并不是在解析器⾥做的，《MySQL 45 讲》说是在解析器做的，但是经过我
和朋友看 MySQL 源码（5.7和8.0）得出结论是解析器只负责检查语法和构建语法树，但是不会去查表或者字段存不
存在。

那到底谁来做检测表和字段是否存在的⼯作呢？别急，接下来就是了。

第四步：执⾏ SQL

经过解析器后，接着就要进⼊执⾏ SQL 查询语句的流程了，每条SELECT 查询语句流程主要可以分为下⾯这三个阶
段：

prepare 阶段，也就是预处理阶段；

optimize 阶段，也就是优化阶段；

execute 阶段，也就是执⾏阶段；

预处理器

我们先来说说预处理阶段做了什么事情。

检查 SQL 查询语句中的表或者字段是否存在；

将 select * 中的 * 符号，扩展为表上的所有列；

我下⾯这条查询语句，test 这张表是不存在的，这时 MySQL 就会在执⾏ SQL 查询语句的 prepare 阶段中报错。

mysql> select * from test;
ERROR 1146 (42S02): Table 'mysql.test' doesn't exist

这⾥贴个 MySQL 8.0 源码来证明表或字段是否存在的判断，不是在解析器⾥做的，⽽是在 prepare 阶段。（PS：
下图是公众号「⼀树⼀溪」⽼哥帮我分析的，这位⽼哥专门写 MySQL 源码⽂章，感兴趣的朋友，可以微信搜索关
注）

上⾯的中间部分是 MySQL 报错表不存在时的函数调⽤栈，可以看到表不存在的错误是在get_table_share()  函数⾥
报错的，⽽这个函数是在 prepare 阶段调⽤的。

不过，对于  MySQL 5.7 判断表或字段是否存在的⼯作，是在词法分析&语法分析之后，prepare 阶段之前做的。结
论都⼀样，不是在解析器⾥做的。代码我就不放了，正因为   MySQL 5.7  代码结构不好，所以 MySQL 8.0 代码结
构变化很⼤，后来判断表或字段是否存在的⼯作就被放⼊到 pr
...
```

## chunk 7

headingPath: [图解MySQL-小林coding-亮白版-v2.0.pdf]

sectionChunkIndex: 8

charRange: 7350-8550

contentLength: 1200

```text
句（select * from product where id = 1）很简单，就是选择使⽤主键索引。

要想知道优化器选择了哪个索引，我们可以在查询语句最前⾯加个 explain 命令，这样就会输出这条 SQL 语句的
执⾏计划，然后执⾏计划中的 key 就表⽰执⾏过程中使⽤了哪个索引，⽐如下图的 key 为 PRIMARY 就是使⽤了主
键索引。

如果查询语句的执⾏计划⾥的 key 为 null 说明没有使⽤索引，那就会全表扫描（type = ALL），这种查询扫描的⽅
式是效率最低档次的，如下图：

这张 product 表只有⼀个索引就是主键，现在我在表中将 name 设置为普通索引（⼆级索引）。

这时 product 表就有主键索引（id）和普通索引（name）。假设执⾏了这条查询语句：

select id from product where id > 1  and name like 'i%';

这条查询语句的结果既可以使⽤主键索引，也可以使⽤普通索引，但是执⾏的效率会不同。这时，就需要优化器来
决定使⽤哪个索引了。

很显然这条查询语句是覆盖索引，直接在⼆级索引就能查找到结果（因为⼆级索引的 B+ 树的叶⼦节点的数据存储
的是主键值），就没必要在主键索引查找了，因为查询主键索引的 B+ 树的成本会⽐查询⼆级索引的 B+ 的成本⼤，
优化器基于查询成本的考虑，会选择查询代价⼩的普通索引。

在下图中执⾏计划，我们可以看到，执⾏过程中使⽤了普通索引（name），Exta 为 Using index，这就是表明使⽤
了覆盖索引优化。

执⾏器

经历完优化器后，就确定了执⾏⽅案，接下来 MySQL 就真正开始执⾏语句了，这个⼯作是由「执⾏器」完成的。
在执⾏的过程中，执⾏器就会和存储引擎交互了，交互是以记录为单位的。

接下来，⽤三种⽅式执⾏过程，跟⼤家说⼀下执⾏器和存储引擎的交互过程（PS ：为了写好这⼀部分，特地去看
MySQL 源码，也是第⼀次看哈哈）。

主键索引查询

全表扫描

索引下推

主键索引查询

以本⽂开头查询语句为例，看看
...
```

